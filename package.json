{
  "name": "codespaces-blank",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs",
    "test": "tests"
  },
  "scripts": {
    "build": "tsc",
    "run-test": "npm run build && k6 run --out json=reports/$(date +%Y%m%d_%H%M%S)_report.json dist/tests/basic-load-test.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/k6": "^1.2.0",
    "typescript": "^5.9.2"
  }
}
